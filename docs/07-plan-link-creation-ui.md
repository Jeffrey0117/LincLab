# Plan 3: 兩階段連結創建 UI/UX 設計文檔

## 目錄
1. [概述](#概述)
2. [用戶流程圖](#用戶流程圖)
3. [線框圖設計](#線框圖設計)
4. [UI 規格說明](#ui-規格說明)
5. [交互細節](#交互細節)
6. [響應式設計](#響應式設計)
7. [組件架構](#組件架構)
8. [技術實現方案](#技術實現方案)
9. [實作步驟](#實作步驟)

---

## 概述

### 設計目標
- 清晰區分兩種連結模式，降低用戶認知負擔
- 提供直觀的模式選擇界面
- 根據模式顯示相應的編輯字段
- 保持視覺一致性和良好的用戶體驗

### 核心概念
採用「漸進式披露」設計模式，將複雜的連結創建過程分為兩個清晰的階段：
1. **模式選擇階段**：用戶選擇創建連結的類型
2. **內容編輯階段**：根據選擇的模式顯示對應的編輯界面

---

## 用戶流程圖

```
┌─────────────────┐
│  點擊新增連結    │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────┐
│        模式選擇畫面                   │
│  ┌──────────┐    ┌──────────┐      │
│  │原創內容  │    │外部連結  │      │
│  │  模式    │    │  模式    │      │
│  └────┬─────┘    └────┬─────┘      │
└───────┼───────────────┼─────────────┘
        │               │
        ▼               ▼
┌──────────────┐  ┌──────────────┐
│原創內容編輯器 │  │外部連結編輯器 │
├──────────────┤  ├──────────────┤
│• HTML 編輯    │  │• 目標網址     │
│• 策略設置     │  │• 預覽摘要     │
│• OG Tags     │  │• 基本信息     │
│• 帳號/標籤    │  │• 帳號/標籤    │
└──────┬───────┘  └──────┬───────┘
       │                  │
       ▼                  ▼
┌──────────────────────────────┐
│        保存並生成連結          │
└──────────────────────────────┘
```

### 狀態流轉圖

```
[初始] ──點擊新增──> [模式選擇]
                        │
            ┌───────────┼───────────┐
            ▼                       ▼
    [原創內容編輯]            [外部連結編輯]
            │                       │
            ├──返回──> [模式選擇] <──返回──┤
            │                       │
            ├──保存──> [成功]  <──保存──┤
            │                       │
            └──取消──> [關閉]  <──取消──┘
```

---

## 線框圖設計

### 階段 1: 模式選擇畫面

```
┌─────────────────────────────────────────────────────────┐
│  ╳                                                      │
├─────────────────────────────────────────────────────────┤
│                                                         │
│              選擇連結類型                                │
│              ─────────────                              │
│              請選擇您要創建的連結類型                      │
│                                                         │
│  ┌─────────────────────────┐  ┌─────────────────────┐  │
│  │      [圖標: 📝]          │  │     [圖標: 🔗]       │  │
│  │                         │  │                      │  │
│  │    原創內容模式          │  │    外部連結模式       │  │
│  │    ───────────          │  │    ───────────       │  │
│  │                         │  │                      │  │
│  │  創建完全自定義的       │  │  引用外部文章並      │  │
│  │  HTML 頁面內容          │  │  添加購買連結        │  │
│  │                         │  │                      │  │
│  │  ✓ 完全控制頁面內容     │  │  ✓ 快速引用外部內容   │  │
│  │  ✓ 自定義策略組件       │  │  ✓ 自動生成預覽卡片   │  │
│  │  ✓ 豐富的交互功能       │  │  ✓ 簡化的編輯流程     │  │
│  │                         │  │                      │  │
│  │  適用於：               │  │  適用於：            │  │
│  │  • 產品詳細介紹         │  │  • 引用評測文章      │  │
│  │  • 教學指南             │  │  • 分享推薦內容      │  │
│  │  • 活動頁面             │  │  • 快速導購頁面      │  │
│  │                         │  │                      │  │
│  │  [   選擇此模式   ]      │  │  [   選擇此模式   ]   │  │
│  └─────────────────────────┘  └─────────────────────┘  │
│                                                         │
│              未來將支援 AI 生成內容模式 🚀                │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 階段 2A: 原創內容模式編輯畫面

```
┌─────────────────────────────────────────────────────────┐
│  ← 返回選擇  原創內容模式                             ╳  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  基本信息                                               │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 連結標題 *                                       │  │
│  │ [________________________________]              │  │
│  │                                                 │  │
│  │ 連結路徑 *                                       │  │
│  │ [shopee-links.com/] [_______________]          │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  頁面內容                                               │
│  ┌─────────────────────────────────────────────────┐  │
│  │ [預覽] [原始碼] [分割視圖]                       │  │
│  │ ┌───────────────────────────────────────────┐   │  │
│  │ │                                           │   │  │
│  │ │         HTML 編輯器區域                    │   │  │
│  │ │         (Monaco Editor)                   │   │  │
│  │ │                                           │   │  │
│  │ └───────────────────────────────────────────┘   │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  策略設置                                               │
│  ┌─────────────────────────────────────────────────┐  │
│  │ ☑ Cookie 彈窗  ☐ 懸浮按鈕  ☐ 下載按鈕           │  │
│  │                                                 │  │
│  │ [策略詳細設置...]                               │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  OG Tags 設置                                           │
│  ┌─────────────────────────────────────────────────┐  │
│  │ OG 標題: [_________________________]            │  │
│  │ OG 描述: [_________________________]            │  │
│  │ OG 圖片: [上傳圖片] 或 [輸入網址___]            │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  分類設置                                               │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 帳號: [選擇帳號 ▼]  標籤: [選擇標籤 ▼]          │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  [取消]                              [預覽] [保存連結]  │
└─────────────────────────────────────────────────────────┘
```

### 階段 2B: 外部連結模式編輯畫面

```
┌─────────────────────────────────────────────────────────┐
│  ← 返回選擇  外部連結模式                             ╳  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  基本信息                                               │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 連結標題 *                                       │  │
│  │ [________________________________]              │  │
│  │                                                 │  │
│  │ 連結路徑 *                                       │  │
│  │ [shopee-links.com/] [_______________]          │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  外部內容設置                                           │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 目標網址 * (要引用的文章或評測)                   │  │
│  │ [_____________________________________________] │  │
│  │                                                 │  │
│  │ [獲取網頁信息] ← 自動抓取標題和描述              │  │
│  │                                                 │  │
│  │ 預覽標題                                        │  │
│  │ [_____________________________________________] │  │
│  │                                                 │  │
│  │ 預覽摘要 (選填，最多 200 字)                     │  │
│  │ ┌───────────────────────────────────────────┐   │  │
│  │ │                                           │   │  │
│  │ │  輸入文章摘要或重點內容...                 │   │  │
│  │ │                                           │   │  │
│  │ └───────────────────────────────────────────┘   │  │
│  │                                  0/200         │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  蝦皮分潤連結                                           │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 商品連結 *                                       │  │
│  │ [_____________________________________________] │  │
│  │ 提示：輸入蝦皮商品連結，系統將自動轉換為分潤連結    │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  預覽卡片                                               │
│  ┌─────────────────────────────────────────────────┐  │
│  │ ┌───────────────────────────────────────────┐   │  │
│  │ │ [預覽圖片]                                 │   │  │
│  │ │                                           │   │  │
│  │ │ 標題預覽                                   │   │  │
│  │ │ 摘要預覽文字...                            │   │  │
│  │ │                                           │   │  │
│  │ │ [查看原文] [立即購買]                      │   │  │
│  │ └───────────────────────────────────────────┘   │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  分類設置                                               │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 帳號: [選擇帳號 ▼]  標籤: [選擇標籤 ▼]          │  │
│  └─────────────────────────────────────────────────┘  │
│                                                         │
│  [取消]                              [預覽] [保存連結]  │
└─────────────────────────────────────────────────────────┘
```

---

## UI 規格說明

### 設計系統
基於 shadcn/ui 和 Tailwind CSS

### 顏色規範

```css
/* 主要顏色 */
--primary: hsl(24 100% 50%);        /* 蝦皮橙色 */
--primary-foreground: hsl(0 0% 100%);

/* 模式選擇卡片 */
--card-hover: hsl(24 100% 95%);     /* 淺橙色背景 */
--card-selected: hsl(24 100% 90%);  /* 選中狀態 */
--card-border: hsl(0 0% 90%);       /* 邊框顏色 */

/* 狀態顏色 */
--success: hsl(142 71% 45%);
--warning: hsl(38 92% 50%);
--error: hsl(0 84% 60%);
--info: hsl(199 89% 48%);
```

### 間距規範

```css
/* 基礎間距 */
--spacing-xs: 0.25rem;  /* 4px */
--spacing-sm: 0.5rem;   /* 8px */
--spacing-md: 1rem;     /* 16px */
--spacing-lg: 1.5rem;   /* 24px */
--spacing-xl: 2rem;     /* 32px */
--spacing-2xl: 3rem;    /* 48px */

/* 組件間距 */
--card-padding: 1.5rem;
--section-gap: 2rem;
--modal-padding: 2rem;
```

### 字體規範

```css
/* 字體大小 */
--text-xs: 0.75rem;     /* 12px */
--text-sm: 0.875rem;    /* 14px */
--text-base: 1rem;      /* 16px */
--text-lg: 1.125rem;    /* 18px */
--text-xl: 1.25rem;     /* 20px */
--text-2xl: 1.5rem;     /* 24px */
--text-3xl: 1.875rem;   /* 30px */

/* 字重 */
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
```

### 組件尺寸

```css
/* 按鈕 */
--button-height-sm: 2rem;    /* 32px */
--button-height-md: 2.5rem;  /* 40px */
--button-height-lg: 3rem;    /* 48px */

/* 輸入框 */
--input-height: 2.5rem;      /* 40px */
--textarea-min-height: 6rem; /* 96px */

/* 卡片 */
--card-width: 20rem;         /* 320px */
--card-max-width: 24rem;     /* 384px */
--modal-width: 40rem;        /* 640px */
--modal-max-width: 56rem;    /* 896px */
```

---

## 交互細節

### 1. 模式選擇卡片

#### Hover 狀態
```css
.mode-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 16px rgba(0,0,0,0.1);
  border-color: var(--primary);
  background: var(--card-hover);
  transition: all 0.2s ease;
}
```

#### Focus 狀態
```css
.mode-card:focus-visible {
  outline: 2px solid var(--primary);
  outline-offset: 2px;
}
```

#### 選中動畫
```javascript
// 點擊卡片時的脈動效果
const handleCardClick = (mode) => {
  // 添加脈動動畫類
  card.classList.add('animate-pulse-once');

  // 延遲 300ms 後進入編輯畫面
  setTimeout(() => {
    navigateToEditor(mode);
  }, 300);
};
```

### 2. 表單驗證

#### 實時驗證
- 路徑輸入：檢查唯一性和格式
- URL 輸入：驗證 URL 格式
- 必填字段：離開焦點時驗證

#### 錯誤提示
```tsx
<FormField error>
  <Input className="border-red-500" />
  <FormMessage className="text-red-500 text-sm mt-1">
    此字段為必填項
  </FormMessage>
</FormField>
```

### 3. 預覽功能

#### 外部連結預覽卡片
- 實時更新預覽內容
- 支持圖片上傳和 URL
- 自動獲取網頁信息

#### 原創內容預覽
- 支持三種視圖模式：預覽、原始碼、分割
- 實時渲染 HTML
- 策略組件預覽

### 4. 鍵盤導航

```javascript
// 支持的快捷鍵
const shortcuts = {
  'Escape': closeDialog,
  'Ctrl+S': saveLink,
  'Ctrl+P': togglePreview,
  'Tab': navigateFields,
  'Shift+Tab': navigateFieldsReverse
};
```

### 5. 載入狀態

```tsx
// 保存時的載入狀態
<Button disabled={isLoading}>
  {isLoading ? (
    <>
      <Loader2 className="mr-2 h-4 w-4 animate-spin" />
      保存中...
    </>
  ) : (
    '保存連結'
  )}
</Button>
```

---

## 響應式設計

### 桌面版 (>= 1024px)
- 模式選擇：並排顯示兩個卡片
- 編輯器：全寬度顯示，側邊預覽
- 最大寬度：896px

### 平板版 (768px - 1023px)
- 模式選擇：並排顯示，卡片稍小
- 編輯器：全寬度，預覽在底部
- 彈性佈局調整

### 手機版 (< 768px)
- 模式選擇：垂直堆疊卡片
- 編輯器：全屏顯示
- 簡化的工具欄
- 底部固定操作按鈕

```css
/* 響應式斷點 */
@media (max-width: 767px) {
  .mode-selection {
    flex-direction: column;
    gap: 1rem;
  }

  .mode-card {
    width: 100%;
  }

  .editor-actions {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 1rem;
    background: white;
    border-top: 1px solid var(--border);
  }
}

@media (min-width: 768px) and (max-width: 1023px) {
  .mode-selection {
    gap: 1.5rem;
  }

  .preview-panel {
    position: relative;
    width: 100%;
    margin-top: 2rem;
  }
}

@media (min-width: 1024px) {
  .editor-container {
    display: grid;
    grid-template-columns: 1fr 400px;
    gap: 2rem;
  }
}
```

---

## 組件架構

### 組件樹結構

```
CreateLinkFlow/
├── ModeSelector/
│   ├── ModeCard
│   └── ModeComparison
├── CustomContentEditor/
│   ├── BasicInfoSection
│   ├── HTMLEditor
│   ├── StrategySelector
│   ├── OGTagsEditor
│   └── CategorySelector
├── ExternalLinkEditor/
│   ├── BasicInfoSection
│   ├── ExternalContentSection
│   │   ├── URLInput
│   │   └── PreviewInput
│   ├── AffiliateSection
│   ├── PreviewCard
│   └── CategorySelector
└── SharedComponents/
    ├── BackButton
    ├── SaveButton
    ├── PreviewModal
    └── ValidationMessages
```

### 狀態管理

```typescript
// 使用 Zustand 進行狀態管理
interface LinkCreationStore {
  // 當前階段
  stage: 'mode-selection' | 'custom-content' | 'external-link';

  // 共用數據
  basicInfo: {
    title: string;
    slug: string;
  };

  // 原創內容數據
  customContent: {
    html: string;
    strategies: Strategy[];
    ogTags: OGTags;
  };

  // 外部連結數據
  externalLink: {
    targetUrl: string;
    preview: {
      title: string;
      summary: string;
      image: string;
    };
    affiliateUrl: string;
  };

  // 分類數據
  category: {
    accountId: string;
    tagIds: string[];
  };

  // Actions
  setStage: (stage: Stage) => void;
  updateBasicInfo: (info: Partial<BasicInfo>) => void;
  updateCustomContent: (content: Partial<CustomContent>) => void;
  updateExternalLink: (link: Partial<ExternalLink>) => void;
  reset: () => void;
  save: () => Promise<void>;
}
```

### 組件接口定義

```typescript
// ModeSelector
interface ModeSelectorProps {
  onSelectMode: (mode: 'custom' | 'external') => void;
}

// ModeCard
interface ModeCardProps {
  mode: 'custom' | 'external';
  title: string;
  description: string;
  features: string[];
  useCases: string[];
  icon: React.ReactNode;
  onSelect: () => void;
}

// CustomContentEditor
interface CustomContentEditorProps {
  initialData?: CustomContentData;
  onSave: (data: CustomContentData) => Promise<void>;
  onCancel: () => void;
  onBack: () => void;
}

// ExternalLinkEditor
interface ExternalLinkEditorProps {
  initialData?: ExternalLinkData;
  onSave: (data: ExternalLinkData) => Promise<void>;
  onCancel: () => void;
  onBack: () => void;
}

// PreviewCard
interface PreviewCardProps {
  title: string;
  summary: string;
  image: string;
  sourceUrl: string;
  affiliateUrl: string;
}
```

---

## 技術實現方案

### 1. Dialog 結構重構

```typescript
// 主對話框組件
export function CreateLinkDialog() {
  const { stage, setStage, reset } = useLinkCreationStore();

  return (
    <Dialog>
      <DialogContent className="max-w-4xl">
        {stage === 'mode-selection' && (
          <ModeSelector onSelectMode={handleModeSelect} />
        )}

        {stage === 'custom-content' && (
          <CustomContentEditor
            onBack={() => setStage('mode-selection')}
            onSave={handleSave}
            onCancel={handleCancel}
          />
        )}

        {stage === 'external-link' && (
          <ExternalLinkEditor
            onBack={() => setStage('mode-selection')}
            onSave={handleSave}
            onCancel={handleCancel}
          />
        )}
      </DialogContent>
    </Dialog>
  );
}
```

### 2. 動畫過渡

```typescript
// 使用 Framer Motion 實現平滑過渡
import { AnimatePresence, motion } from 'framer-motion';

const stageVariants = {
  enter: { x: 300, opacity: 0 },
  center: { x: 0, opacity: 1 },
  exit: { x: -300, opacity: 0 }
};

<AnimatePresence mode="wait">
  <motion.div
    key={stage}
    variants={stageVariants}
    initial="enter"
    animate="center"
    exit="exit"
    transition={{ duration: 0.3 }}
  >
    {renderStageContent()}
  </motion.div>
</AnimatePresence>
```

### 3. 表單驗證

```typescript
// 使用 React Hook Form + Zod
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import * as z from 'zod';

// 原創內容模式驗證
const customContentSchema = z.object({
  title: z.string().min(1, '標題為必填'),
  slug: z.string().min(1, '路徑為必填').regex(/^[a-z0-9-]+$/, '路徑格式無效'),
  html: z.string().min(1, 'HTML 內容為必填'),
  strategies: z.array(z.any()),
  ogTags: z.object({
    title: z.string().optional(),
    description: z.string().optional(),
    image: z.string().url().optional()
  })
});

// 外部連結模式驗證
const externalLinkSchema = z.object({
  title: z.string().min(1, '標題為必填'),
  slug: z.string().min(1, '路徑為必填').regex(/^[a-z0-9-]+$/, '路徑格式無效'),
  targetUrl: z.string().url('請輸入有效的 URL'),
  affiliateUrl: z.string().url('請輸入有效的蝦皮連結'),
  preview: z.object({
    title: z.string().optional(),
    summary: z.string().max(200, '摘要最多 200 字').optional(),
    image: z.string().optional()
  })
});
```

### 4. API 整合

```typescript
// API 調用封裝
class LinkAPI {
  // 創建原創內容連結
  static async createCustomLink(data: CustomContentData) {
    return fetch('/api/links/custom', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data)
    });
  }

  // 創建外部連結
  static async createExternalLink(data: ExternalLinkData) {
    return fetch('/api/links/external', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data)
    });
  }

  // 獲取網頁信息（用於外部連結）
  static async fetchPageInfo(url: string) {
    return fetch('/api/utils/fetch-page-info', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ url })
    });
  }

  // 驗證路徑唯一性
  static async validateSlug(slug: string) {
    return fetch(`/api/links/validate-slug?slug=${slug}`);
  }
}
```

### 5. 實用工具函數

```typescript
// utils/link-creation.ts

// 自動生成路徑
export function generateSlug(title: string): string {
  return title
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-|-$/g, '')
    .substring(0, 50);
}

// 驗證蝦皮連結
export function isShopeeLink(url: string): boolean {
  return /shopee\.(tw|com|vn|my|sg|th|ph|id)/.test(url);
}

// 轉換為分潤連結
export function convertToAffiliateLink(url: string, affiliateId: string): string {
  // 實現分潤連結轉換邏輯
  return `${url}?af_id=${affiliateId}`;
}

// 預處理 HTML 內容
export function sanitizeHTML(html: string): string {
  // 使用 DOMPurify 清理 HTML
  return DOMPurify.sanitize(html, {
    ALLOWED_TAGS: ['div', 'span', 'p', 'h1', 'h2', 'h3', 'img', 'a', ...],
    ALLOWED_ATTR: ['class', 'id', 'href', 'src', 'alt', ...]
  });
}
```

---

## 實作步驟

### 第一階段：基礎架構 (2-3 天)

1. **建立狀態管理**
   - 實現 Zustand store
   - 定義數據模型
   - 設置初始狀態

2. **創建基礎組件結構**
   - CreateLinkDialog 容器
   - 基本路由邏輯
   - 階段切換機制

3. **實現模式選擇畫面**
   - ModeSelector 組件
   - ModeCard 組件
   - 基本樣式和動畫

### 第二階段：編輯器開發 (3-4 天)

4. **原創內容編輯器**
   - 遷移現有編輯器組件
   - 添加返回按鈕
   - 整合表單驗證

5. **外部連結編輯器**
   - 創建新的編輯器組件
   - 實現 URL 抓取功能
   - 預覽卡片組件

6. **共用組件**
   - BasicInfoSection
   - CategorySelector
   - ValidationMessages

### 第三階段：功能完善 (2-3 天)

7. **表單驗證和錯誤處理**
   - 整合 React Hook Form
   - 實現 Zod 驗證規則
   - 錯誤提示 UI

8. **預覽功能**
   - 實時預覽更新
   - 預覽模態框
   - 響應式預覽

9. **API 整合**
   - 連接後端 API
   - 處理載入狀態
   - 錯誤處理

### 第四階段：優化和測試 (2 天)

10. **性能優化**
    - 組件懶加載
    - 表單防抖
    - 圖片優化

11. **響應式設計**
    - 手機版適配
    - 平板版適配
    - 測試各種設備

12. **用戶體驗優化**
    - 添加過渡動畫
    - 鍵盤快捷鍵
    - 無障礙支持

### 第五階段：部署和監控 (1 天)

13. **測試**
    - 單元測試
    - 集成測試
    - E2E 測試

14. **部署**
    - 環境配置
    - 部署腳本
    - 監控設置

---

## 未來擴展

### AI 生成內容模式（第三種模式）

```
┌─────────────────────┐
│    AI 生成模式       │
│    ───────────      │
│                     │
│  使用 AI 自動生成    │
│  優質的產品介紹      │
│                     │
│  ✓ 一鍵生成內容      │
│  ✓ SEO 優化         │
│  ✓ 多語言支持        │
│                     │
│  適用於：            │
│  • 批量創建頁面      │
│  • 快速內容生產      │
│  • SEO 優化頁面      │
└─────────────────────┘
```

### 功能增強建議

1. **模板系統**
   - 預設模板庫
   - 自定義模板
   - 模板市場

2. **協作功能**
   - 多人編輯
   - 版本控制
   - 審核流程

3. **分析整合**
   - 實時預覽流量
   - A/B 測試
   - 轉換率追蹤

4. **批量操作**
   - 批量創建
   - 批量編輯
   - 批量導入/導出

---

## 結論

這個兩階段的連結創建流程設計：

1. **提升用戶體驗**：清晰的模式選擇降低認知負擔
2. **提高效率**：根據模式顯示相應字段，避免混亂
3. **易於擴展**：架構支持未來添加新模式
4. **視覺一致**：遵循 shadcn/ui 設計系統
5. **技術可行**：基於現有技術棧，易於實現

通過漸進式披露和模式分離，用戶可以更直觀地創建適合自己需求的連結，同時保持界面的簡潔和易用性。